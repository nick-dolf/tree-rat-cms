<ul class="list-group">
  <% for (let i =0; i < page.files.length; i++) { const file = page.files[i]; %>
    <%- include("../components/file-row", {file, extraClass:"bg-info", index: `new${i}`}) %>
  <% } %>
</ul>